import { ReactiveFormsModule } from '@angular/forms';
import { TextFieldModule } from '@angular/cdk/text-field';
// import { DynamicValidatorMessage } from '../form/dynamic-validator-message.directive';

export const formsModule = [ReactiveFormsModule, TextFieldModule];
